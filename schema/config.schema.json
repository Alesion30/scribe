{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "scribe configuration",
  "description": "Configuration file schema for the scribe CLI.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference URL."
    },
    "model": {
      "type": "string",
      "description": "Whisper model name or absolute path. When a name is given, scribe looks for ~/.scribe/models/{name}.bin.",
      "default": "large-v3-turbo",
      "examples": ["large-v3-turbo", "tiny", "/path/to/custom-model.bin"]
    },
    "recordingDir": {
      "type": "string",
      "description": "Default output directory for recordings. Defaults to ~/.scribe/recordings/.",
      "default": "~/.scribe/recordings"
    },
    "language": {
      "type": "string",
      "description": "Language hint for transcription (ISO 639-1). Set to 'auto' for automatic detection.",
      "examples": ["ja", "en", "zh"],
      "default": "auto"
    },
    "format": {
      "type": "string",
      "description": "Transcript output format. 'srt' and 'vtt' include segment timestamps.",
      "enum": ["txt", "srt", "vtt"],
      "default": "txt"
    },
    "noMic": {
      "type": "boolean",
      "description": "Disable microphone input and capture system audio only.",
      "default": false
    },
    "noSystem": {
      "type": "boolean",
      "description": "Disable system audio and capture microphone input only.",
      "default": false
    },
    "vad": {
      "type": "boolean",
      "description": "Skip silence with a Silero VAD model before transcribing. Prevents whisper from looping on long silent stretches. The VAD model is downloaded on first use.",
      "default": true
    },
    "chunkLength": {
      "type": "number",
      "description": "Seconds of audio per whisper call. whisper normalizes the spectrogram across its whole input, so feeding a long recording at once flattens quiet speech. Set to 0 to disable splitting.",
      "minimum": 0,
      "default": 600
    }
  },
  "additionalProperties": false
}
